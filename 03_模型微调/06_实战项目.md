# 实战项目

## 客服助手微调

### 项目概述
```
目标：构建智能客服助手
技术：QLoRA + 对话数据
特点：礼貌、专业、解决问题
```

### 数据准备
```python
# 客服对话数据格式
customer_service_data = [
    {
        "instruction": "回答用户关于退货的问题",
        "input": "我买的东西不满意，可以退货吗？",
        "output": "您好！我们支持7天无理由退货。请在订单详情页申请退货，我们会尽快为您处理。"
    },
    {
        "instruction": "回答用户关于物流的问题",
        "input": "我的订单什么时候能到？",
        "output": "您好！请提供您的订单号，我可以帮您查询物流状态。通常配送时间为3-5个工作日。"
    }
]
```

### 微调代码
```python
from transformers import AutoModelForCausalLM, AutoTokenizer, BitsAndBytesConfig
from peft import LoraConfig, get_peft_model
from trl import SFTTrainer, SFTConfig

# 1. 量化配置
bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_quant_type="nf4",
    bnb_4bit_compute_dtype=torch.float16
)

# 2. 加载模型
model = AutoModelForCausalLM.from_pretrained(
    "Qwen/Qwen2-7B",
    quantization_config=bnb_config,
    device_map="auto"
)

# 3. LoRA配置
lora_config = LoraConfig(
    r=16,
    lora_alpha=32,
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj"],
    lora_dropout=0.1,
    bias="none"
)

# 4. 应用LoRA
model = get_peft_model(model, lora_config)

# 5. 训练配置
training_args = SFTConfig(
    output_dir="./customer_service_model",
    num_train_epochs=3,
    per_device_train_batch_size=2,
    gradient_accumulation_steps=4,
    learning_rate=2e-4,
    fp16=True,
    logging_steps=10,
    save_strategy="epoch"
)

# 6. 训练
trainer = SFTTrainer(
    model=model,
    train_dataset=dataset,
    args=training_args
)

trainer.train()

# 7. 保存
model.save_pretrained("./customer_service_lora")
```

## 代码助手微调

### 项目概述
```
目标：构建代码生成助手
技术：QLoRA + 代码数据
特点：代码质量高，注释详细
```

### 数据准备
```python
# 代码数据格式
code_data = [
    {
        "instruction": "写一个Python函数，计算斐波那契数列",
        "output": """def fibonacci(n: int) -> int:
    '''计算第n个斐波那契数
    
    Args:
        n: 斐波那契数的位置
        
    Returns:
        第n个斐波那契数
    '''
    if n <= 1:
        return n
    
    a, b = 0, 1
    for _ in range(2, n + 1):
        a, b = b, a + b
    
    return b"""
    }
]
```

### 微调配置
```python
# 代码专用LoRA配置
lora_config = LoraConfig(
    r=32,  # 代码任务需要更高的秩
    lora_alpha=64,
    target_modules=[
        "q_proj", "k_proj", "v_proj", "o_proj",
        "gate_proj", "up_proj", "down_proj"
    ],
    lora_dropout=0.05,
    bias="none"
)

# 训练配置
training_args = SFTConfig(
    output_dir="./code_assistant_model",
    num_train_epochs=5,  # 代码任务需要更多轮次
    per_device_train_batch_size=2,
    gradient_accumulation_steps=8,
    learning_rate=1e-4,
    fp16=True,
    optim="adamw_torch",
    max_grad_norm=1.0
)
```

## 创意助手微调

### 项目概述
```
目标：构建创意写作助手
技术：QLoRA + 创意文本
特点：想象力丰富，风格多样
```

### 数据准备
```python
# 创意写作数据
creative_data = [
    {
        "instruction": "写一首关于春天的诗",
        "output": """春风轻抚大地醒，
嫩芽初绽绿意浓。
蝴蝶翩翩花间舞，
万物复苏生机萌。"""
    },
    {
        "instruction": "写一个科幻短故事开头",
        "output": """2150年，人类已经殖民火星。艾拉是第一批在火星出生的人类，她从未见过地球的蓝天。今天，她收到了一个来自地球的包裹，里面装着一颗种子——一颗地球上最后的苹果树种子..."""
    }
]
```

### 微调配置
```python
# 创意任务配置
lora_config = LoraConfig(
    r=64,  # 创意任务需要更高的表达能力
    lora_alpha=128,
    target_modules=["q_proj", "v_proj", "k_proj", "o_proj"],
    lora_dropout=0.1,
    bias="none"
)

# 训练参数
training_args = SFTConfig(
    output_dir="./creative_assistant_model",
    num_train_epochs=10,  # 创意任务需要更多训练
    per_device_train_batch_size=2,
    gradient_accumulation_steps=4,
    learning_rate=1e-4,
    warmup_ratio=0.1,
    fp16=True
)
```

## 完整项目示例

### 项目结构
```
llm_finetune_project/
├── data/
│   ├── train.json
│   ├── val.json
│   └── test.json
├── configs/
│   ├── model_config.yaml
│   └── training_config.yaml
├── scripts/
│   ├── prepare_data.py
│   ├── train.py
│   └── evaluate.py
├── models/
│   └── saved_models/
└── results/
    └── evaluation_results/
```

### 完整训练脚本
```python
# train.py - 完整训练脚本
import argparse
from transformers import (
    AutoModelForCausalLM,
    AutoTokenizer,
    BitsAndBytesConfig,
    TrainingArguments
)
from peft import LoraConfig, get_peft_model, prepare_model_for_kbit_training
from trl import SFTTrainer
from datasets import load_dataset

def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--model_name", type=str, default="Qwen/Qwen2-7B")
    parser.add_argument("--data_path", type=str, required=True)
    parser.add_argument("--output_dir", type=str, required=True)
    parser.add_argument("--epochs", type=int, default=3)
    parser.add_argument("--batch_size", type=int, default=2)
    parser.add_argument("--learning_rate", type=float, default=2e-4)
    args = parser.parse_args()
    
    # 1. 量化配置
    bnb_config = BitsAndBytesConfig(
        load_in_4bit=True,
        bnb_4bit_quant_type="nf4",
        bnb_4bit_compute_dtype=torch.float16
    )
    
    # 2. 加载模型
    model = AutoModelForCausalLM.from_pretrained(
        args.model_name,
        quantization_config=bnb_config,
        device_map="auto"
    )
    
    tokenizer = AutoTokenizer.from_pretrained(args.model_name)
    tokenizer.pad_token = tokenizer.eos_token
    
    # 3. 准备模型
    model = prepare_model_for_kbit_training(model)
    
    # 4. LoRA配置
    lora_config = LoraConfig(
        r=16,
        lora_alpha=32,
        target_modules=["q_proj", "v_proj"],
        lora_dropout=0.1,
        bias="none"
    )
    
    model = get_peft_model(model, lora_config)
    
    # 5. 加载数据
    dataset = load_dataset("json", data_files=args.data_path)
    
    # 6. 训练参数
    training_args = TrainingArguments(
        output_dir=args.output_dir,
        num_train_epochs=args.epochs,
        per_device_train_batch_size=args.batch_size,
        gradient_accumulation_steps=4,
        learning_rate=args.learning_rate,
        fp16=True,
        logging_steps=10,
        save_strategy="epoch"
    )
    
    # 7. 训练
    trainer = SFTTrainer(
        model=model,
        train_dataset=dataset["train"],
        args=training_args
    )
    
    trainer.train()
    
    # 8. 保存
    model.save_pretrained(f"{args.output_dir}/final_model")
    tokenizer.save_pretrained(f"{args.output_dir}/final_model")
    
    print("训练完成！")

if __name__ == "__main__":
    main()
```

## 评估方法

### 代码评估
```python
def evaluate_code_model(model, tokenizer, test_cases):
    """评估代码生成模型"""
    
    scores = []
    
    for test_case in test_cases:
        # 生成代码
        inputs = tokenizer(test_case["instruction"], return_tensors="pt")
        outputs = model.generate(**inputs, max_new_tokens=200)
        generated_code = tokenizer.decode(outputs[0], skip_special_tokens=True)
        
        # 评估代码质量
        score = {
            "syntax": check_syntax(generated_code),  # 语法正确性
            "execution": test_execution(generated_code, test_case["test_cases"]),  # 执行正确性
            "quality": evaluate_code_quality(generated_code)  # 代码质量
        }
        
        scores.append(score)
    
    return scores
```

### 对话评估
```python
def evaluate_chat_model(model, tokenizer, conversation_tests):
    """评估对话模型"""
    
    metrics = {
        "relevance": [],  # 相关性
        "fluency": [],    # 流畅性
        "safety": [],     # 安全性
        "helpfulness": [] # 有帮助性
    }
    
    for test in conversation_tests:
        response = generate_response(model, tokenizer, test["input"])
        
        metrics["relevance"].append(evaluate_relevance(response, test["input"]))
        metrics["fluency"].append(evaluate_fluency(response))
        metrics["safety"].append(evaluate_safety(response))
        metrics["helpfulness"].append(evaluate_helpfulness(response, test["input"]))
    
    # 计算平均值
    avg_metrics = {k: sum(v)/len(v) for k, v in metrics.items()}
    
    return avg_metrics
```

## 部署建议

### 1. 模型合并
```python
# 合并LoRA权重
from peft import PeftModel

base_model = AutoModelForCausalLM.from_pretrained("Qwen/Qwen2-7B")
model = PeftModel.from_pretrained(base_model, "./lora_weights")
merged_model = model.merge_and_unload()

# 保存合并后的模型
merged_model.save_pretrained("./merged_model")
```

### 2. 量化部署
```python
# 4-bit量化部署
from transformers import BitsAndBytesConfig

quantization_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_quant_type="nf4"
)

model = AutoModelForCausalLM.from_pretrained(
    "./merged_model",
    quantization_config=quantization_config,
    device_map="auto"
)
```

## 学习资源
- Hugging Face微调教程
- TRL文档
- LoRA实战指南

## 下一步
继续学习 [常见问题解答](07_常见问题.md)